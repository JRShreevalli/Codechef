Statement :
            You are given an integer N. Consider the sequence containing the integers 1,2,…,N 
            in increasing order (each exactly once). Find the maximum length of its contiguous 
            subsequence with an even sum . 
            
Problem Code: MXEVNSUB

LInk : https://www.codechef.com/LP1TO201/problems/MXEVNSUB

// SOLUTION 

#include <iostream>
using namespace std;

int main() 
{
	int  t , n , i , add = 0  ; 
	cin >> t ;
	while ( t-- )
	{
	    cin >> n ;
	    int  add = n * (n + 1) / 2 ;
	    if ( add % 2 == 0 )
	        cout << n << endl ;
	    else 
	        cout << n-1 << endl  ;
	}
	return 0;
}
